tools calling:
strict: true it force the model to return exact pamrameters of the function but in the schema all parameters have to be required or set to bull
and dditionalfield should set to flalse in openai but in calude wont' throw an error.